/*let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
let total = 0
let contagem = 0*/

/*for (let i = 0; i < numbers.length; ++i) {
    console.log(numbers[i])
    total += numbers[i]
    console.log(total)
    console.log(total/numbers.length)
}*/

/*for (let i = 0; i < numbers.length; ++i) {
    if (numbers[i] > total) {
        total = numbers[i]
    } else {
        total = total
    }
}

console.log(total)

if (numbers.pop() > 20){
    console.log('Valor maior que 20!')
} else {
    console.log('Valor menor ou igual a 20!')
}

let arrayNum = []

for (let i = 1; i <= 25; ++i) {
    arrayNum.push(i)
}

console.log(arrayNum)

for (let i = 0; i < arrayNum.length; ++i) {
    console.log(arrayNum[i]/2)
}*/
/*let resultado = 1
let num = 10

for (let i = num; i > 1; i--) {
    resultado = resultado * i
}

console.log(resultado)

let word = 'tryber';
let invertedWord = []

for (let i = word.length - 1; i >= 0; --i) {
    invertedWord.push(word[i])
}

console.log(String(invertedWord))*/

/*let array = ['java', 'javascript', 'python', 'html', 'css'];

let totalChar = 0
let biggestWord = []

for (let i = 0; i < array.length; ++i) {
    if (array[i].length > totalChar) {
        totalChar = array[i].length
        biggestWord.push(array[i])
    } else {
        totalChar = totalChar
    }
}

console.log(biggestWord.pop())*/
/*let array = ['java', 'javascript', 'python', 'html', 'css'];

let smallestWord = []
let totalChar = 1000

for (let i = 0; i < array.length; ++i) {
    if (array[i].length < totalChar) {
        totalChar = array[i].length
        smallestWord.push(array[i])
    } else {
        totalChar = totalChar
    }
}

console.log(smallestWord.pop())

let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

console.log(numbers.sort(function(a, b) {return a-b}))
console.log(numbers.sort(function(a, b) {return b-a}))*/

